spring:
  datasource:
    url: jdbc:sqlite:D:/sqlite-tools-win32-x86-3420000/test
    driver-class-name: org.sqlite.JDBC
  redis:
    host: 127.0.0.1
    port: 6379
    timeout: 6000ms

mybatis:
  mapperLocations: classpath:mapper/*.xml

mybatis-plus:
  configuration:
    jdbc-type-for-null: 'null'

